import { defineConfig } from "vite";
import UserscriptPlugin from "vite-userscript-plugin";

export default defineConfig({
    plugins: [
        UserscriptPlugin({
            entry: "src/main.ts",
            fileName: "npu-ng",
            header: {
                name: "Neptun PowerUP (NG)",
                author: "Keve Lajsz (klevcsoo)",
                version: JSON.stringify(process.env.npm_package_version).replace(/"/g, ""),
                match: ["https://*neptun*/*"],
            },
        }),
    ],
});
